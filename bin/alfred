#!/usr/bin/env ruby

alfred = "#{File.dirname(__FILE__)}/../lib/alfred"

begin
  require alfred
rescue LoadError
  require 'rubygems'
  require alfred
end

puts 'Starting Alfred...'

Alfred::App.run!({
  :environment => :production
})
