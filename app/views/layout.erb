<!DOCTYPE html>
<html>
  <head>
    <meta http-equiv="Content-type" content="text/html; charset=utf-8" />

    <link href="/stylesheets/application.css" media="screen" rel="stylesheet" type="text/css" />

    <script src="/javascripts/jquery-2.0.2.min.js" type="text/javascript"></script>
    <script src="/javascripts/application.js" type="text/javascript"></script>

    <title>Alfred : <%= @hostname %></title>
  </head>

  <body>
    <div id="menu">
      <a id="menu-toggle" href="#menu">+ Commands</a><br />
      <!--
      <div>
        <label for="refresh_page">Refresh Page:</label>
        <input type="checkbox" id="refresh_page" />
        every <input type="text" id="refresh_interval" value="10" size="3" /> seconds
      </div>
      -->
      <div>
        &rarr;&nbsp;<a href="<%= url('/') %>">Dashboard</a><br />
      </div>

      <% unless @commands.nil? %>
        <div>
          <% @commands.each do |command| %>
            &rarr;&nbsp;<a href="<%= url("/command/#{command.id}") %>"><%= command.name %></a>
            <br />
          <% end %>
        </div>
      <% end %>
    </div>

    <div id="status">
      Alfred v<%= Alfred::VERSION %>
      <br />
      Ruby <%= RUBY_VERSION %> / Sinatra <%= Sinatra::VERSION %>
      <br />
      Generated on: <em><%= Time.now %></em>
    </div>

    <div id="page">
      <%= yield %>
    </div>
  </body>
</html>
